import React from 'react';
import PropTypes from 'prop-types';

const CharactersListContainer = (props) => {
  return <div>{props.mainCharacters}</div>;
};

CharactersListContainer.propTypes = {};

export default CharactersListContainer;
